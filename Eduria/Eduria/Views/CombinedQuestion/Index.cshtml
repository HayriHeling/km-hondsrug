@model IEnumerable<Eduria.Models.CombinedQuestionAnswer>

@{
    ViewData["Title"] = "Index";
}

<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QuestionModel.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QuestionModel.Text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AnswerModels[0].Text)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionModel.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionModel.Text)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AnswerModels.FindAll(o => o.QuestionId == item.QuestionModel.Id).Find(o => o.CorrectAnswer).Text)
            </td>
            <td>
            </td>
        </tr>
    }
    </tbody>
</table>